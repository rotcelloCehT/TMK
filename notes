1. npx create-react-app name-app
2. Make globalStyles.js in a 'style' folder
        -Remember to add the global body styles.
3. Install npm install --save styled-components
4. Import Navigation bar from ressources.
    - hooks.js
    - npm install --save 'usesate' AND  'useref'
    - theme provider
5. - HOW TO MAKE MENU DESKTOP : css properties to adjust menu based on screen size.
6. -conditional rendering of burger based on screen size 
        -https://stackoverflow.com/questions/36862334/get-viewport-window-height-in-reactjs
7. - COMPONENTS CONDITION RENDERING 
        -https://reactjs.org/docs/conditional-rendering.html
        -https://stackoverflow.com/questions/42083181/is-it-possible-to-return-empty-in-react-render-function
8. -GALLERY 
        -remove space between flex containers
        -!!!! FIGURE OUT HOW TO REMOVE THE SPACING BETWEEN FLEXBOX ITEMS

9. BODY NO DISPLAY FLEX;
        -with: 100%; box-sizing: border-box; 
        -COMPONENTS DISPLAY:FLEX;

        emergency reset 
        drivers
        in vantage: battery gauge

10. CHANGE PYROGRPAHY AND CHANGE THE FONT ON "CUSTOM MADE" !!!!!!!!!!!!!!!!!!!!!!!!!!

11. GOOGLE MAPS
        - https://www.digitalocean.com/community/tutorials/how-to-integrate-the-google-maps-api-into-react-applications
        - map styles position: absolute;
        - map container that wrap the component map you import, position: relative;

12. CONTACT FORM react
        - https://w3collective.com/react-contact-form/

ALERT: font-size: 0; in globalStyles breaks the burger 

13. using href="#example" and id="example". USE scroll-behavior: smooth; on global body,html tag for smoothness

14. Menu and Burger hooks modification. 
        -if (!ref.current || ref.current.contains(event.target) && event.target.tagName.toLowerCase() !== 'a')
        - if !componentReceived OR clicked inside the component (NAV) AND the TARGET is not one of the links,
                -return nothing
        -ELSE return the mouse down event.